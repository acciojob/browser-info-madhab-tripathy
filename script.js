//your JS code here. If required.
const browser = document.getElementById("browser-info");
const info = `You are using ${window.navigator.appName} version ${window.navigator.appVersion}`
browser.innerText = info